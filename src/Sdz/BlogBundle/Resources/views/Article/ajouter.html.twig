<h3>Création d Article</h3>

<div class="well">
<form action="{{ path('article_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit" class="btn btn-primary">Créer cet article</button>
    </p>
</form>
</div>

<ul class="record_actions">
    <li>
        <a href="{{ path('article_index') }}">
            Retour &agrave; la liste
        </a>
    </li>
</ul>
